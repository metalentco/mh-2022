title: Oberseite mit Weiterleitung

columns:
  main:
    width: 2/3
    sections:
      fields:
        type: fields
        fields:
          forwarding:
            label: Weiterleitung
            type: select
            options: query
            query: page.children.published
  sidebar:
    width: 1/3
    sections:
      pages:
        type: pages
        template: default

